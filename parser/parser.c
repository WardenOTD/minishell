#include "ms.h"

/*
int parser(t_lexer *lexer)
{
	t_commandtable *commands;
	char	*tmp;
	int		i;

	tmp = NULL;
	while (lexer)
	{
//		commands->cmd = ft_strdup(lexer->token[i]);
		lexer = lexer->next;
	}
	printf("%s\n", commands->cmd[0]);
}
*/